export const metadata = {
  title: 'Documentation — RackEditor',
  description:
    'Public documentation for the RackEditor selective pallet rack design and quoting platform.',
};

export default function DocsLayout({ children }) {
  return children;
}
